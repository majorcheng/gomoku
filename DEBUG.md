# 30×30 搜索性能核验

## Observations

- 环境：本机 Node v26.8.2；浏览器 Worker 与主线程独立；无第三方运行依赖。
- 旧版 `scorePoint` 每次对两个颜色反复扫描四方向五格窗口；候选排序为完整插入排序。
- `vcfSearch` 有节点/深度上限，没有时间检查；`negamax` 每 1024 节点查时限。
- `negamax` 抛出 ABORT 时，`undoMove` 不在 finally 中；根节点 catch 会混用不同深度的分数。
- 以上为静态观察；下面用只扩展 SIZE/CENTER 的旧引擎副本做动态实验。

## Hypotheses

### H1：重复点评分是主要耗时（ROOT HYPOTHESIS）
- 支持：候选生成后每节点对全部候选调用两次窗口评分。
- 反证：完整排序和整线增量评估也可能占主要时间，尚未采样。
- 实验：不改搜索逻辑，CPU 采样固定局面的深度 4 搜索，比较各函数采样占比。

### H2：大棋盘主要慢在候选排序
- 支持：完整插入排序是 O(n²)，但只使用前 12–32 个候选。
- 反证：实际对局可能集中，候选数未必随面积线性增加。
- 实验：同一 CPU 采样核对排序与点评分的占比，记录候选数。

### H3：VCF 忽略时限、ABORT 未撤子造成思考超时及状态残留
- 支持：搜索代码缺少 VCF deadline 检查及 try/finally。
- 反证：普通简单局面可能提前返回，没有触发这些路径。
- 实验：仅在临时 VM 添加只读状态快照，使用可控时钟触发超时，检查时间读取和着法栈是否恢复。

## Experiments

- H1 确认：30×30 集中/分散局面各 6 次深度 4 搜索，CPU 采样合计 415.729 ms，其中 `scorePoint` 224.138 ms（53.9%）。集中局面 72 个候选、约 22 ms；分散局面 194 个候选、约 41 ms。
- H2 仅部分支持：`negamax` 自身约 73 ms，包含排序及调度，低于评分；排序优先改为前 K 个候选的有界插入排序，保留原顺序和宽度。
- H3 确认：临时 VM 只增加只读状态快照与可控时钟；在第 1024 节点触发 ABORT，输入 14 手，搜索返回后的着法栈为 16 手。VCF 必胜局面仅在开始/返回读取时间，报告 10 ms 但预算为 1 ms；旧 VCF 节点统计为 0。
- 禁手扫描基线：30×30 的 8860 次空点检查耗时约 289 ms；其四/三判断仅需检查包含原点的五格窗口，整条线扫描可限制为原点正负 4 格。
- 实验未改变生产搜索逻辑；旧副本、CPU 采样保存在临时目录 `/var/folders/v2/hghnmwm91yx2cbdr9wf3dk9m0000gp/T/gomoku30-eqr4wkl1/`。

## Root Cause

搜索反复计算未变化的候选棋型；VCF 未检查 deadline，递归 ABORT 又越过撤子语句，造成超时后局面残留。

## Fix

局部失效的评分缓存、前 K 候选排序、三/四检查限制在有效窗口；VCF 共用时间预算，递归使用 finally 撤子，超时保留最后完整层结果。多个挡点搜索中断时也保留已完成的挡点评分。

## Verification

- `node scripts/check.mjs` 通过：900 个坐标往返、扩展区域判胜/悔棋、900 点和棋、禁手、必胜/必挡、正常与超时后的棋盘/哈希/评估/评分缓存一致性、VCF 节点统计。
- `node scripts/check.mjs <临时目录>/baseline.cjs`：两局面各 2 次预热、5 次测量，中位数集中局面 20.387 → 15.057 ms（1.35×），分散局面 44.946 → 19.402 ms（2.32×）；每次完整候选、分数、节点数与旧引擎一致。
- 新旧禁手裁判对照：确定性随机 20 盘，共 15,076 个空点，判定全部一致且检查后棋盘未变化；累计耗时 429.047 → 80.651 ms。
- 真实时钟 25 ms 预算、深度上限 12、VCF 开启：集中/分散/单边冲四局面分别 25.332 / 25.138 / 25.136 ms 返回，均为完整深度 3、有限候选评分。时限按检查点执行，允许单次检查间隔的小幅超时。
- `git diff --check`、引擎语法检查通过；本地 HTTP 返回 200、正确 MIME 与 no-store。
- 浏览器自动化通道没有提供可用浏览器；真实页面点击和视觉验收未完成。未重新校准 30×30 六档难度胜率。
- 此阶段实际 push 远端为 GitHub，当时没有提交/推送授权，修改保留在本地；后续授权和连接状态见 `tasks/todo.md` 的 Git 交付记录。

## 宗师棋力修复（用户已确认实施）

### 观察与依据

- 宗师仍为 12 层上限、4 秒、32/16 普通候选、零随机失误；新旧参数相同。
- 合法历史 `K15 AD30 L15 AD28 M15 AD26 O15 AD24 P15 AD22` 后，旧搜索把 N15 长连当作 win1；主线程剔除唯一候选后改下 A30。
- 交叉跳活三 P15 被裁判漏判；Q15、P14 都能合法延伸成两端成五的活四。
- 已读取 RIF 官方规则 https://www.renju.net/rifrules/ 第 3、9 节：活三只需一个合法延伸，不能同时成五；假活三需递归排除；恰五优先于禁手。
- VCF 耗尽总预算可让普通搜索停在深度 0；固定 50% 规则会提前放弃后续迭代。
- 叶子的一步赢/对手双端四仅得到约 +910030/-910030 的静态分，没有识别确定胜负。
- 两个被白棋隔开的眠三被整线窗口计数错误合成 90000 分，应分别计 3000 分。

### 修复与验证

- 原生 module Worker 导入共享裁判，修复真假活三、恰五优先，所有搜索落子和 VCF 攻防遵守同一规则；主线程删除随意落角兜底。
- 先完成基础搜索，VCF 子预算为 `min(400 ms, 剩余时间 × 20%)`；子预算耗尽不置整手 aborted，继续普通搜索。完整层结果与 finally 撤子保留。
- 节点优先处理合法赢/挡，高级及以上保留真实冲四并在基础两层后最多延伸 4 手强制变化；静态分不能跨入胜负阈值。
- CPU 采样显示 scoreLine 约占 35%、scorePoint 约占 25%、规则计算约占 25%。缓存旧线分后只重算变化后的四条线，并与全盘重算比较；没有引入依赖评分半径的禁手缓存。
- 眠三按对手棋子分隔的片段独立计分，相关样例现在为 6000 分。
- `node scripts/check.mjs` 已通过：规则、搜索逐手合法性审计、真假活三递归、合法必胜/必挡、静态/线分缓存、全局与 VCF 子预算、叶子战术及候选裁切、真实 ES module 在线程中的消息协议、错误后恢复。
- Bridge 测试替身只用于加载失败、取消与过期 seq 路由；真实 Node 线程使用未改写的引擎模块，仅适配消息 API。两者均不代表浏览器视觉验收。
- 成对对弈完成，结果见 `tasks/strength-matches.json`；对照方为已完成规则/预算修复的 phase12 版本，双方同规则同 4 秒预算，12 个开局换先，共 24 局。
- phase12 与当前对弈版本 phase3 的完整源码快照保存在 `/var/folders/v2/hghnmwm91yx2cbdr9wf3dk9m0000gp/T/gomoku-strength-m17udlu9/`，对弈报告记录引擎 SHA-256。
- 当前版 19 胜、对照版 4 胜、1 局（central_8 当前执黑）追加 80 手后未结束，0 错误。双方合计 827 次搜索均合法，最大耗时 4005.34 ms，非直接成五的深度 0 返回为 0。
- 已独立重放全部 24 局、827 个搜索落子，核对唯一开局/执子组合、合法性、终局及赢家。未结束局未记为和棋。固定开局小样本不能推断通用胜率或 Elo。
- 对弈后仅同步注释与界面说明，算法未变化；最终 `node scripts/check.mjs`、所有 JS/MJS 语法检查及 `git diff --check` 通过。浏览器点击/视觉验收仍未完成。
